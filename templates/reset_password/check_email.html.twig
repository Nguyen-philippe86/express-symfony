{% extends 'base.html.twig' %}

{% block title %}Email de réinitialisation du mot de passe{% endblock %}

{% block body %}
{{ include("partials/title_section.html.twig", {'name':'Email de réinitialisation du mot de passe'}) }}

    <div class="login_register_wrap section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-xl-6 col-md-10">
                    <div class="login_wrap">
                        <div class="padding_eight_all bg-white">
                            <div class="heading_s1">
                                <h3>Réinitialiser le mot de passe</h3>
                            </div>
                            <div class="content">
                                <p>
                                    Un e-mail vient d'être envoyé contenant un lien que vous pouvez utiliser pour réinitialiser votre mot de passe.
                                    Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                                </p>
                                <p>Si vous ne recevez pas d'e-mail, veuillez vérifier votre dossier spam ou <a href="{{ path('app_forgot_password_request') }}">Réessayer</a>.</p>
                            </div>
                            <div class="form-note text-center">Vous n'avez pas encore de compte ? <a href="{{path('app_register')}}">Créer un compte</a></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
